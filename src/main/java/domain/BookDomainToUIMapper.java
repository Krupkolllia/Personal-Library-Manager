package domain;

import common.Mapper;
import presentation.BookUI;

public class BookDomainToUIMapper implements Mapper<BookDomain, BookUI> {
    @Override
    public BookUI map(BookDomain from) {
        return null;
    }
}
